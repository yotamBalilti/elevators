import React from "react";
import { ReactComponent as SVGIcon } from "../Elevator/elevator.svg";

const Icon = ({ width, color }) => (
  <SVGIcon title="elevator" className="icon" width={width} color={"#FFF12E"} />
);

export default Icon;
